# ROADMAP.md — 미니 언어모델 만들기 로드맵

## 목표
아주 작은 규모라도 **실제로 작동하는 언어모델**(글을 넣으면 다음 글자를 예측해서 새 텍스트를 만들어내는 것)을 완전히 처음부터 직접 만든다. 목적은 속도나 성능이 아니라, "AI가 왜·어떻게 작동하는지"를 스스로 설명할 수 있는 상태가 되는 것.

## 전체 흐름

| 단계 | 무엇을 만드나 | 왜 필요한가 | 확인 방법 |
|---|---|---|---|
| 0. 환경 준비 | Python 가상환경 + numpy | 코드를 실행할 자리 마련 | `python3 -c "import numpy"` 에러 없음 |
| 1. 인공 뉴런 하나 | 가중치·문턱값 있는 뉴런 1개 (책 2장 내용 그대로 코드화) | 신경망의 최소 단위 이해 | 입력에 따라 0/1 출력이 손으로 계산한 값과 일치 |
| 2. 신경망 쌓기 | 여러 뉴런을 층으로 쌓은 forward pass (아직 학습 없음, 무작위 가중치) | "여러 뉴런 = 신경망"이 실제로 어떻게 연결되는지 | 입력 → 출력까지 행렬 연산으로 한 번에 계산됨 |
| 3. 학습이란 무엇인가 | 손실 함수 + 경사하강법 + 역전파를 직접 구현 | 책이 "피드백으로 가중치를 조정한다"고만 하고 넘어간 부분 — 실제로 **어떻게** 조정하는지 | 간단한 문제(AND/OR 게이트)를 신경망이 스스로 학습해서 맞힘 |
| 3+. (추가) 학습 후 해석가능성 실습 | 학습이 끝난 뒤, 뉴런들이 실제로 뭘 배웠는지 직접 까서 분석 | "블랙박스"가 정말 못 보는 건지, 아니면 그냥 안 들여다본 건지 우리 눈으로 확인 | 학습된 가중치를 출력해서 의미를 유추하고, 가능하면 AI(Claude)에게 활성화 패턴을 보여주고 자연어로 설명을 생성시켜봄 (자동화된 해석가능성 실습) |
| 4. 비선형 활성화 함수 | 계단 함수 대신 sigmoid/ReLU | 책에서 언급한 "비선형성"의 실체, 복잡한 패턴 학습의 전제조건 | 함수 그래프 확인 + 신경망에 적용 후에도 정상 동작 |
| 5. 진짜로 학습하는 신경망 | 은닉층 있는 MLP로 XOR 문제 학습 | "왜 신경망에 여러 층이 필요한가"를 직접 겪어보기 (역사적으로도 중요한 문제) | loss가 점점 줄어들고, XOR을 정확히 맞힘 |
| 6. 텍스트를 다루는 법 | 문자 단위 토큰화 + 임베딩(글자를 숫자 벡터로) | 지금까지는 숫자만 다뤘음 — 언어를 숫자로 바꾸는 다리 | 문장을 넣으면 숫자 배열로, 되돌리면 원래 문장으로 |
| 7. 최초의 언어모델 | 바이그램(직전 글자 하나만 보고 다음 글자 예측) 모델 | "언어모델"이라는 개념 자체를 가장 단순한 형태로 체험 | 학습 후 그럴듯한(완전 랜덤보다 나은) 글자열 생성 |
| 8. 어텐션 | self-attention 한 블록 | 트랜스포머의 핵심 아이디어 — "문맥에서 어떤 부분에 집중할지" | 어텐션 가중치를 출력해서 실제로 의미 있는 곳에 집중하는지 관찰 |
| 9. 미니 트랜스포머 조립 | 임베딩 + 어텐션 + 순전파층을 쌓은 작은 트랜스포머 | 지금까지 만든 조각을 모두 연결 | 모델 구조 요약 출력, forward pass 정상 동작 |
| 10. 학습 & 생성 — "나만의 AI" | 작은 텍스트로 미니 트랜스포머를 학습시키고 새 텍스트 생성 | 이 프로젝트의 최종 목표 | 학습 전엔 의미 없는 글자, 학습 후엔 원본과 비슷한 스타일의 글 생성 |
| 11. (선택) 여기서 더 크게 가려면 | 실제 GPT/Claude 규모와의 차이 정리 | 책의 "특이점" 이야기와 우리가 만든 것의 관계를 이해 | 문서로 정리, 코드 추가 없음 |

## 준비물

- Mac (M1 이상) — 이미 있음
- Python 3.11 이상 (버전 확인 필요)
- 가상환경 (venv)
- numpy — 처음부터 끝까지 사용
- matplotlib — 선택, loss 그래프를 눈으로 보고 싶을 때
- PyTorch — 8단계부터, 이유는 아래 "설계 결정" 참고
- 코드 편집기 — Xcode 대신 VS Code 등 추천 (Python엔 Xcode가 안 맞음)
- 학습용 텍스트 데이터 — 7단계에서 정함 (짧은 영어/한글 글)

## 설계 결정 (확정)

- **역전파**: 3~5단계는 순수 Python/numpy로 직접 구현(원리 체득). 8단계 트랜스포머부터는 PyTorch 자동미분(autograd) 사용 — 실제 업계도 이렇게 배우고, 트랜스포머 역전파를 손으로 짜는 건 비효율적이라는 판단.
- **학습 데이터**: 7단계에서 결정.

## 이 책과의 관계

『마침내 특이점이 시작된다』는 "왜 신경망이 지능을 만드는가"라는 큰 그림을 준다. 우리가 만드는 건 그 큰 그림의 가장 작은 실물 버전이다. 책 2장(44~51쪽)에서 나온 개념 — 신경세포, 시냅스 강도(가중치), 발화 문턱값, 토폴로지, 학습(피드백으로 강도 조정), 비선형성, 동기식/비동기식 — 은 1~4단계에서 그대로 코드로 만나게 된다.

- 2026-09-12: Stage 3 학습 완료 후 "해석가능성 실습"을 로드맵에 추가 — 학습된 뉴런을 직접 분석하고, AI로 AI를 분석하는 것(자동화된 해석)도 시도해보기로 함.

## 히스토리
- 2026-09-11: 로드맵 작성. 책 2장(44~51쪽) 내용 반영.
