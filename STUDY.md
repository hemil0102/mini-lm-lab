# STUDY.md

용어집과 질문/답변을 한 파일에 누적합니다.

## 용어집

| 용어 | 뜻 | 비유 | 나온 곳 |
|---|---|---|---|
| 신경세포(뉴런) | 입력 여러 개를 받아 하나의 출력(0 또는 1)을 내는 최소 계산 단위 | 여러 사람의 의견(입력)을 듣고 찬성표 수가 기준을 넘으면 "가결"을 외치는 위원 한 명 | 책 2장, 44쪽 |
| 시냅스 강도(가중치, weight) | 연결마다 붙는 숫자 — 그 입력을 얼마나 중요하게 볼지 | 위원마다 표에 곱해지는 영향력 배수 (영향력 큰 사람 의견은 더 세게 반영) | 책 2장, 44·51쪽 |
| 발화 문턱값(threshold) | 입력 가중치 합이 이 값을 넘어야 뉴런이 "켜진다"(출력 1) | 안건 가결에 필요한 최소 찬성 점수 | 책 2장, 44·47쪽 |
| 토폴로지(topology) | 뉴런들이 몇 층으로, 어떻게 연결돼 있는지의 전체 구조 | 조직도 — 누가 누구에게 보고하는지 | 책 2장, 44·45쪽 |
| 비선형성(nonlinearity) | 출력이 입력에 정비례하지 않고 "확 켜지거나 꺼지는" 식으로 반응하는 성질 | 스위치 — 전압을 조금씩 올려도 반응 없다가 임계점에서 갑자기 켜짐 | 책 2장, 50쪽 |
| 동기식/비동기식 신경망 | 동기식: 0층부터 M층까지 순서대로 계산 / 비동기식: 각 뉴런이 독립적으로 계속 자기 입력을 확인하며 스스로 발화 | 동기식은 순서대로 진행하는 회의, 비동기식은 각자 알아서 판단하고 말하는 단체 채팅방 | 책 2장, 51쪽 |
| 진화 알고리즘 | 배선(연결)이나 초기 가중치를 무작위 변이 + 선택으로 최적화하는 방법 | 자연선택을 흉내내서 "더 나은 설계"가 살아남게 하는 것 | 책 2장, 48·50쪽 |
| 층(layer) | 같은 입력을 보는 뉴런 여러 개의 묶음. 층의 출력은 뉴런 개수만큼의 숫자(벡터) | 같은 안건을 동시에 심사하는 여러 위원회 | Stage 2, 책 44쪽 그림 |
| 행렬곱(@, matrix multiplication) | 여러 뉴런의 가중합을 반복문 없이 한 번에 계산하는 연산 | 여러 계산기를 동시에 돌리는 것 — 뉴런 수가 많아질수록 이 방식이 훨씬 빠름 | Stage 2 |

## Q&A

(질문과 핵심 답변을 여기에 이어서 기록)

**Q. 행렬곱(inputs @ weights)이 뭔지, 은닉층 뉴런이 뭔지 초등학생 수준으로 설명해줘**
A. 행렬곱은 "여러 뉴런의 계산표를 한 번에 훑는 것"이다 — 뉴런마다 따로따로 계산기를 두드리는 대신, 모든 뉴런의 가중치를 표(열)로 늘어놓고 입력을 그 표에 한 번에 곱해서 더하면 모든 뉴런의 가중합이 동시에 나온다. 뉴런이 2개든 3000개든 "표를 한 번 훑는" 모양은 똑같아서, 이런 "곱하고 더하기"를 동시에 잘하는 하드웨어(GPU)가 신경망에 딱 맞는다.
은닉층 뉴런은 입력층(맨 처음 값)도 출력층(맨 마지막 답)도 아닌, 중간에서 "미리 생각"해주는 뉴런이다. 뉴런 하나로는 AND/OR 같은 단순한 판단만 가능한데, 여러 중간 판단(은닉층)을 거쳐야 더 복잡한 결론(출력층)을 낼 수 있다.
직접 설계한 예: 은닉층 3개 = [AND뉴런, OR뉴런, "숙제만 보는" 뉴런]. 입력=(1,0)일 때 AND=0(숙제만으론 부족), OR=1(하나라도 했으니 통과), 숙제전용=1(숙제를 봤으니 통과) → 은닉층 출력=[0,1,1]. 출력뉴런은 "은닉층 3개 중 2개 이상 켜지면 허락"(가중치 전부 1, threshold 1.5) → 0+1+1=2 > 1.5 → 최종 출력=1. 모두 손 계산과 일치.
