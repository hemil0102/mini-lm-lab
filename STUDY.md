# STUDY.md

용어집과 질문/답변을 한 파일에 누적합니다.

## 용어집

| 용어 | 뜻 | 비유 | 나온 곳 |
|---|---|---|---|
| 신경세포(뉴런) | 입력 여러 개를 받아 하나의 출력(0 또는 1)을 내는 최소 계산 단위 | 여러 사람의 의견(입력)을 듣고 찬성표 수가 기준을 넘으면 "가결"을 외치는 위원 한 명 | 책 2장, 44쪽 |
| 시냅스 강도(가중치, weight) | 연결마다 붙는 숫자 — 그 입력을 얼마나 중요하게 볼지 | 위원마다 표에 곱해지는 영향력 배수 (영향력 큰 사람 의견은 더 세게 반영) | 책 2장, 44·51쪽 |
| 발화 문턱값(threshold) | 입력 가중치 합이 이 값을 넘어야 뉴런이 "켜진다"(출력 1) | 안건 가결에 필요한 최소 찬성 점수 | 책 2장, 44·47쪽 |
| 토폴로지(topology) | 뉴런들이 몇 층으로, 어떻게 연결돼 있는지의 전체 구조 | 조직도 — 누가 누구에게 보고하는지 | 책 2장, 44·45쪽 |
| 비선형성(nonlinearity) | 출력이 입력에 정비례하지 않고 "확 켜지거나 꺼지는" 식으로 반응하는 성질 | 스위치 — 전압을 조금씩 올려도 반응 없다가 임계점에서 갑자기 켜짐 | 책 2장, 50쪽 |
| 동기식/비동기식 신경망 | 동기식: 0층부터 M층까지 순서대로 계산 / 비동기식: 각 뉴런이 독립적으로 계속 자기 입력을 확인하며 스스로 발화 | 동기식은 순서대로 진행하는 회의, 비동기식은 각자 알아서 판단하고 말하는 단체 채팅방 | 책 2장, 51쪽 |
| 진화 알고리즘 | 배선(연결)이나 초기 가중치를 무작위 변이 + 선택으로 최적화하는 방법 | 자연선택을 흉내내서 "더 나은 설계"가 살아남게 하는 것 | 책 2장, 48·50쪽 |
| 층(layer) | 같은 입력을 보는 뉴런 여러 개의 묶음. 층의 출력은 뉴런 개수만큼의 숫자(벡터) | 같은 안건을 동시에 심사하는 여러 위원회 | Stage 2, 책 44쪽 그림 |
| n차원 배열(input shape) | 입력 데이터를 숫자들의 격자로 표현한 것 — 몇 차원(n)을 쓸지는 데이터의 자연스러운 구조를 따른다 | 표(1D)냐, 바둑판(2D)이냐, 정육면체(3D)냐 — 담을 내용물 모양에 맞춰 그릇을 고르는 것 | 책 45쪽 |
| 스펙트로그램(spectrogram) | 소리(원래 1차원 신호)를 주파수별로 쪼개서 만든 2차원 배열 — 세로=주파수 성분, 가로=시간 | 오케스트라 악보 — 세로축은 어떤 음(음높이), 가로축은 언제 연주하는지(시간) | 책 45쪽 |
| 행렬곱(@, matrix multiplication) | 여러 뉴런의 가중합을 반복문 없이 한 번에 계산하는 연산 | 여러 계산기를 동시에 돌리는 것 — 뉴런 수가 많아질수록 이 방식이 훨씬 빠름 | Stage 2 |
| CPU vs GPU | CPU=적은 수의 똑똑한 코어로 복잡한 일을 순서대로 처리 / GPU=수천 개의 단순한 코어로 같은 단순 연산을 동시에 처리 | CPU=혼자 다양한 일을 처리하는 팀장, GPU=단순 작업을 동시에 하는 수천 명의 작업장 | GPU/RAM 수요 질문 |
| VRAM | GPU 전용 메모리 — 수천 개 코어가 동시에 읽고 쓸 수 있도록 일반 RAM보다 훨씬 빠른 통로로 연결됨 | GPU 작업장 바로 옆에 붙은 전용 창고 | GPU/RAM 수요 질문 |
| 지도학습(supervised learning) | 정답이 붙어있는 예시 데이터(입력+정답)로 신경망을 학습시키는 방식 | 답안지가 있는 문제집으로 공부하는 것 | Stage 3 시작 |
| 손실/오차(loss/error) | 신경망의 출력과 진짜 정답이 얼마나 다른지를 숫자 하나로 나타낸 것 | 시험 채점 후 틀린 정도 점수 | Stage 3 시작 |
| 경사하강법(gradient descent) | 오차가 줄어드는 방향으로 가중치를 조금씩 바꿔가는 학습 방법 | 눈 감고 산에서 발밑 기울기만 느끼며 골짜기로 한 발씩 내려가는 것 | Stage 3 시작 |
| 역전파(backpropagation) | 출력층에서 생긴 오차를 입력층 방향으로 거꾸로 흘려보내며 각 층 가중치의 책임을 계산하는 알고리즘 | 결과가 잘못됐을 때 마지막 단계부터 거꾸로 누가 얼마나 잘못했는지 되짚어가는 것 | Stage 3 시작 |

## Q&A

(질문과 핵심 답변을 여기에 이어서 기록)

**Q. 행렬곱(inputs @ weights)이 뭔지, 은닉층 뉴런이 뭔지 초등학생 수준으로 설명해줘**
A. 행렬곱은 "여러 뉴런의 계산표를 한 번에 훑는 것"이다 — 뉴런마다 따로따로 계산기를 두드리는 대신, 모든 뉴런의 가중치를 표(열)로 늘어놓고 입력을 그 표에 한 번에 곱해서 더하면 모든 뉴런의 가중합이 동시에 나온다. 뉴런이 2개든 3000개든 "표를 한 번 훑는" 모양은 똑같아서, 이런 "곱하고 더하기"를 동시에 잘하는 하드웨어(GPU)가 신경망에 딱 맞는다.
은닉층 뉴런은 입력층(맨 처음 값)도 출력층(맨 마지막 답)도 아닌, 중간에서 "미리 생각"해주는 뉴런이다. 뉴런 하나로는 AND/OR 같은 단순한 판단만 가능한데, 여러 중간 판단(은닉층)을 거쳐야 더 복잡한 결론(출력층)을 낼 수 있다.
직접 설계한 예: 은닉층 3개 = [AND뉴런, OR뉴런, "숙제만 보는" 뉴런]. 입력=(1,0)일 때 AND=0(숙제만으론 부족), OR=1(하나라도 했으니 통과), 숙제전용=1(숙제를 봤으니 통과) → 은닉층 출력=[0,1,1]. 출력뉴런은 "은닉층 3개 중 2개 이상 켜지면 허락"(가중치 전부 1, threshold 1.5) → 0+1+1=2 > 1.5 → 최종 출력=1. 모두 손 계산과 일치.

**Q. 학습시키면서 증거를 남겨두면 신경망을 분석할 수 있지 않나? "블랙박스"라는데 사실 분석 가능할 것 같다. 그럼 이해하기 어려운 걸 AI로 다시 분석하면 되지 않나?**
A. 맞는 방향이다 — 이미 있는 연구 분야고 이름도 있다: 해석가능성(interpretability), 최근엔 메커니즘적 해석가능성. "블랙박스"는 정확한 표현이 아니다 — 가중치 숫자는 전부 꺼내볼 수 있어서 안 보이는 게 아니라, 숫자가 너무 많고(수십억 개) 사람이 이해할 이름표가 안 붙어있어서 어려운 것. 실제 방법: 활성화 기록(뉴런이 어떤 입력에 반응하는지 로그), 어텐션 시각화(8단계에서 우리도 함), 특정 개념에 대응하는 뉴런 조합 찾기. "AI로 AI를 분석"하는 것도 실제로 하는 방법(자동화된 해석가능성) — 예: 언어모델에게 한 뉴런의 활성화 패턴을 보여주고 "이 뉴런이 뭘 담당하는지" 자연어로 설명하게 시킴. 다만 이 방법도 완벽하진 않음 — 분석하는 AI의 설명 자체가 틀렸거나 그럴듯하게만 보일 수 있어서 검증이 더 필요함. 우리 프로젝트는 신경망이 작아서 실제로 모든 뉴런을 다 들여다볼 수 있음 — Stage 3 학습 끝나면 직접 해보기로 함.

**Q. 행렬곱이 실제로 어떤 계산 과정인지 더 풀어서, 은닉 뉴런은 어떻게 생긴 건지**
A. 행렬곱은 "표의 각 열마다 (입력×같은행 가중치)를 다 더하는 것"을 열 개수만큼 반복하는 것뿐이다. 입력(1,0), 표[[1,1],[1,1]]일 때 AND열: 1×1+0×1=1, OR열: 1×1+0×1=1 — 따로 계산한 것과 결과가 완전히 같다. 다른 점은 "각 열마다 반복하라"는 지시를 사람이 하나씩 안 하고 `@` 한 번으로 시킨다는 것뿐 — 열이 2개든 3000개든 이 반복 규칙은 그대로라서 GPU(곱하고 더하기를 대량 동시에 잘하는 부품)에 잘 맞는다.
은닉 뉴런은 구조적으로 일반 뉴런과 완전히 동일하다(가중치 곱해서 더하고 threshold와 비교). "은닉"이라는 이름은 순전히 위치 때문 — 입력층도 출력층도 아닌 중간에 있다는 뜻. 지난 예시(AND/OR/숙제전용)처럼 사람이 미리 의미를 정해준 건 이해를 돕기 위한 특별 설계였고, 실제로는 무작위로 시작해서 학습(3단계)을 거치며 의미가 저절로 생겨난다.

**Q. 책 45쪽 "문제 입력" — 시각 패턴은 왜 2차원 배열이고, 청각은 왜 2차원 배열인지 시각화해서 보고 싶음. 지금까지 쓴 [0,1] 입력이랑 책의 입력 형태는 어떤 관계인지. 다른 입력 형태도 궁금함**
A. 이미지가 2차원인 이유: 픽셀이 실제로 가로·세로 2방향으로 배열돼 있어서, 그 구조를 숫자 배열에 그대로 반영한 것 (input_shapes_demo.png 왼쪽 — 6x6 배열의 숫자 하나하나가 픽셀 밝기와 정확히 대응).
소리가 2차원인 이유는 이미지와 다르다: 소리 자체는 원래 시간에 따른 값 하나짜리 1차원 신호인데, 분석하기 좋게 "이 순간에 어떤 주파수가 얼마나 섞여있나"로 쪼개서 2차원(주파수 x 시간, 스펙트로그램)으로 바꾼 것 (오른쪽 그림 — 시간이 지나며 저음에서 고음으로 올라가는 소리를 표현).
지금까지 쓴 [0,1] 같은 입력은 책이 말한 "n차원 배열"에서 n=1인 가장 단순한 경우(그냥 숫자 리스트, 벡터)다. 실제로 2차원 이미지를 우리가 만든 것 같은 기본 신경망(1~2단계)에 넣으려면 보통 한 줄로 "펼쳐서(flatten)" 1차원으로 바꿔 넣는다 — 이미지 전용 신경망(CNN, 나중 주제)은 2차원 구조를 유지한 채 처리하지만 그건 더 나중 얘기.
책에 없는 다른 입력 형태: 컬러 이미지는 사실 3차원(높이x너비x색상채널 R/G/B 3장), 동영상은 4차원(시간x높이x너비x색상채널), 텍스트는 문자/단어를 숫자로 바꾼 1차원 나열(우리 프로젝트 6~7단계에서 다룰 것), 시계열(주가·센서값)도 1차원, 표 형태 데이터(스프레드시트 한 행)는 우리 AND게이트 입력 [숙제,청소]를 특성 여러 개로 늘린 것과 같은 1차원 벡터.

**Q. 책에서 말하는 "신경망 중간층"이 은닉 뉴런인가?**
A. 그렇다. 책 44쪽 그림의 흐름은 입력점(뉴런 아님, 그냥 값) → 신경망 1층 → 신경망 중간층 → 출력층 → 출력. 입력점과 출력층을 뺀 "신경망 1층"과 "신경망 중간층"의 뉴런들이 전부 은닉 뉴런이다. "중간층"과 "은닉층(hidden layer)"은 같은 대상을 가리키는 다른 이름일 뿐.

**Q. 책에서 말한 토폴로지 정의가 이해 안됨 — "입력이 다른 신경세포의 출력 또는 문제 입력 값 중 하나와 연결"이 뭔 뜻인지, "높은 층"에 역할이 있는지, 시냅스 강도는 무작위 배정인지 연결인지**
A. 토폴로지 문장을 그대로 풀면: 각 뉴런의 입력 한 칸 한 칸은 (a) 다른 뉴런의 출력, 또는 (b) 원본 문제 입력값, 둘 중 하나에 연결된다. 그리고 각 뉴런의 출력 하나는 (a) 보통 더 나중 층에 있는 다른 뉴런의 입력, 또는 (b) 신경망 전체의 최종 출력, 둘 중 하나가 된다. topology_demo.png가 정확히 이 구조 — 입력 2개(원본 값) → 은닉 뉴런 3개(AND/OR/숙제전용, 이들의 입력=원본 입력, 출력=다음 층 뉴런의 입력) → 출력 뉴런 1개(입력=은닉층 출력들, 출력=최종 결과).
"높은 층"은 고정된 역할이 있는 게 아니라 순서/위치를 말하는 것 — 몇 번째로 계산되느냐일 뿐. 다만 실제로 학습된 큰 신경망을 보면 경험적으로 낮은 층은 단순한 패턴(선, 경계 같은 것), 높은 층은 그걸 조합한 복잡한 패턴을 잡는 경향이 관찰됨(이것도 해석가능성 연구 주제).
시냅스 강도(가중치)와 연결(토폴로지)은 서로 다른 개념 — 연결은 "누가 누구랑 선으로 이어지는지"(배선), 가중치는 "그 선에 붙는 숫자"(얼마나 세게 반영할지). 일반적으로는 둘 다 무작위로 시작할 수 있지만, 우리 프로젝트에서는 배선은 항상 고정(완전연결, 모든 입력이 모든 뉴런과 연결)이고 가중치 숫자만 설계하거나(지금까지) 학습으로 바뀐다(3단계부터). topology_demo.png의 점선 화살표(입력2→숙제전용, w=0)가 그 예 — 선으로는 연결돼 있지만 가중치가 0이라 사실상 무시되는 경우.

**Q. 딥러닝 때문에 왜 램/그래픽카드 수요가 늘었는지, CPU는 아예 쓸모없어진 건지**
A. 신경망 계산의 정체는 결국 "가중합을 엄청 많이, 동시에" 계산하는 것 — 실제 언어모델 하나의 가중치 개수는 수십억~수천억 개(지금 우리 layer.py의 2x2 표와 비교하면 규모가 전혀 다름).
CPU는 코어(계산 두뇌)가 몇 개~수십 개뿐이지만 코어 하나하나가 복잡한 일(조건 분기, 여러 작업 번갈아 처리)을 잘하도록 설계됨. GPU는 원래 게임 화면의 픽셀 수백만 개를 동시에 칠하려고 만든 부품이라, 복잡한 일 하나를 잘하는 대신 단순한 곱하고-더하기 같은 일을 코어 수천 개로 한꺼번에 처리하도록 설계됨.
신경망 한 층의 뉴런 여러 개의 가중합은 서로 의존하지 않아서(1번 뉴런 계산이 2번 뉴런을 기다릴 필요 없음) GPU의 수천 개 코어에 나눠주면 전부 동시에 끝남 — CPU는 코어가 적어서 순서대로 하느라 훨씬 오래 걸림.
램/VRAM 수요 증가 이유: 계산하려면 필요한 숫자(가중치, 입력, 중간 결과)가 전부 빠른 메모리 위에 올라가 있어야 하는데, 모델이 커질수록 이 숫자 자체가 많아짐. 학습할 때(3단계 역전파)는 나중에 오차를 거꾸로 계산하려고 각 층의 중간 결과(활성화값)까지 기억해둬야 해서 메모리 수요가 한 번 더 늘어남. GPU가 일반 RAM과 별도로 전용 메모리(VRAM)를 갖는 이유도 수천 개 코어가 동시에 읽고 쓰려면 훨씬 빠른 통로가 필요하기 때문.
CPU가 쓸모없어진 건 아니고 역할이 나뉜 것 — CPU는 여전히 전체 프로그램 흐름 결정, 데이터 준비(파일 읽기, 전처리), 그리고 GPU에게 "이 행렬 곱해"라고 지시 내리는 일을 담당. GPU는 스스로 프로그램을 시작하지 못하고 CPU가 시키는 단순 반복 계산만 초고속으로 처리하는 전문 일꾼. 비유: CPU=다양한 일을 혼자 처리 가능한 팀장, GPU=복잡한 판단은 못 하지만 똑같은 단순 작업을 수천 명이 동시에 하는 작업장.

**Q. (오개념 확인) 은닉층이 결정을 랜덤화하는 건가? 그리고 입력을 넣으면 가중치에 따라 학습이 되는 건가 — 고양이 사진을 넣었을 때 신경망이 어떤 기준으로 계속 "고양이"라고 알 수 있게 학습하는지. 책에 이 내용이 있나?**
A. 오개념 정정: 은닉층은 "무작위성을 더하는 것"이 아니라 "단순 판단들을 조합해서 더 복잡한 판단을 가능하게 하는 중간 단계"다. 무작위인 건 학습 시작 전 가중치의 "초기값"일 뿐 — 은닉층 자체의 성질이 아님.
책 확인: 지금까지 본 44~51쪽엔 학습(가중치가 실제로 어떻게 바뀌는지)의 구체적 방법은 없음. 책이 큰 그림 위주라 개념적으로만 짧게 다룰 가능성이 높음 — 처음부터 계획한 3단계("학습이란 무엇인가")에 해당하는 질문이라 책과 무관하게 직접 설계해서 실습하기로 함.
학습 원리(지도학습): 신경망 혼자서는 정답을 모름 — 정답이 붙은 예시 데이터(고양이 사진=정답 "고양이")가 반드시 필요. 흐름: ① 초기엔 가중치가 무작위라 출력도 사실상 랜덤 ② 출력과 정답을 비교해 "얼마나 틀렸는지"(오차/손실, loss)를 숫자로 계산 ③ 오차를 줄이려면 각 가중치를 어느 방향·얼마나 바꿀지 알아야 하는데, 이걸 구하는 방법이 경사하강법(gradient descent) — 눈 감고 산에서 발밑 기울기만 느끼며 골짜기(오차 최소점)로 한 발씩 내려가는 것에 비유. 가중치는 "위치", 오차는 "고도".
층이 여러 개일 때는 출력층에서 생긴 오차를 이전 층들까지 거슬러 올라가며 "이 가중치가 오차에 얼마나 책임 있었나"를 계산해야 하는데, 이를 효율적으로 하는 알고리즘이 역전파(backpropagation) — 오차 신호를 출력층→입력층 방향으로 거꾸로 흘려보내며 각 층의 가중치 책임을 순서대로 계산.
이 과정을 수천~수백만 장의 예시에 반복하면 가중치가 "고양이 사진에 자주 나오는 패턴엔 크게 반응, 무관한 패턴엔 반응 안 하도록" 점점 자리잡음 — "고양이가 뭔지 이해"하는 게 아니라 "이 숫자 패턴 → 정답이 고양이였던 경우가 많았다"를 통계적으로 맞춰가는 것.
