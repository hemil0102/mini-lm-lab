# CLAUDE.md

이 저장소에서 AI 어시스턴트가 지켜야 할 규칙입니다. (다른 Side Project 저장소들의 규칙을 이 프로젝트 성격에 맞게 옮긴 것)

## 작업 규칙

1. **코드는 바로 줍니다 — 질문으로 끌어내지 않습니다.** 이번 단계에서 «막힐 요소»를 먼저 예고하고, 그 아래에 «따라 칠 코드» 전문을 한 번에 둡니다. 코드 뒤에는 **왜 이렇게 썼는지**를 붙입니다. 앞 단계와 같은 패턴은 반복 설명하지 않고, 이번에 **처음 나오는 것**만 짚습니다.
2. **처음 보는 개념을 문제로 던지기 전에, 답할 수 있을 만큼 먼저 설명합니다** — ① 무엇인가 ② 왜 있는가 ③ 선택지가 무엇인가. 「모른다」로만 답할 수 있는 질문은 내지 않습니다.
3. **문제는 개념 중심입니다.** 단순 계산·자릿수 같은 것은 문제로 내지 않고 그냥 알려줍니다.
4. **사용자는 Python/머신러닝 초보, Swift/iOS는 능숙합니다.** Python 문법·numpy·PyTorch 같은 것도 "이미 알 것"으로 두지 않고 처음 나올 때 설명합니다. 반대로 일반 프로그래밍 개념(변수, 함수, 반복문 등)은 굳이 설명하지 않습니다. **(2026-09-14 추가) 코드를 줄 때마다 그 코드에 나온 파이썬 고유 문법(f-string, `if __name__ == "__main__":`, 슬라이싱, 컴프리헨션 등)을 코드 안 주석 + 코드 뒤 설명으로 짚어줍니다** — Swift에 대응 문법이 있으면 그것과 비교해서 설명.
5. **한 번에 한 단계씩 진행합니다.** 앞 단계가 실제로 동작하는 것을 (숫자·출력으로) 확인하기 전에는 다음 단계로 넘어가지 않습니다.
6. **각 단계에는 「눈으로 확인하는 방법」이 따라야 합니다.** 코드가 에러 없이 실행되는 것은 확인이 아닙니다 — loss 값, 생성된 텍스트, 출력 shape 등 «내 설명이 틀렸다면 드러날» 숫자/결과를 함께 둡니다.
7. **세션을 시작할 때 [PROGRESS.md](PROGRESS.md) 를 먼저 읽고, 진척이 생기면 그 자리에서 갱신합니다.**
8. **막혔거나 질문한 것은 요약해서 [STUDY.md](STUDY.md) 에 누적합니다** (질문 + 핵심 답변, 용어집 겸용). 아직 못 푼 것은 [PROBLEMS.md](PROBLEMS.md) 에 두고, 풀리면 STUDY.md 로 옮깁니다.
9. **책 사진에서 나온 내용은 핵심만 우리말로 정리해서 기록합니다.** 책 원문을 통째로 옮겨 적거나 저장소에 이미지로 저장해두지 않습니다 — 저작권 있는 책이므로 이해를 위한 요약/재구성만 남깁니다.
10. **어느 페이지/챕터를 다루고 있는지 STUDY.md·PROGRESS.md에 남깁니다.** 나중에 "그거 몇 챕터였지" 하고 되짚을 수 있게.
11. **설명이나 코드를 줄 때는 「핵심」을 맨 먼저, 10문장 이내로.** 자세한 설명은 그 뒤에 잇습니다.
12. **목표는 "따라만 치는" 것이 아니라 "왜 이렇게 동작하는지 스스로 설명할 수 있는" 상태입니다.** 책이 얼버무리고 넘어가는 부분은 짚고 넘어갑니다.

---

## 프로젝트 정보

책/매뉴얼을 따라가며 아주 작은 실제 언어모델(미니 LM)을 직접 구현해보는 학습 프로젝트입니다.

- **목표**: 클로드/챗GPT 같은 것이 "왜" 작동하는지 이해하고, 최소 형태로나마 실제로 작동하는 언어모델을 직접 코드로 구현
- **책 제목**: 『마침내 특이점이 시작된다』 (레이 커즈와일, 비즈니스북스) — 기술 매뉴얼이 아니라 큰 그림/비전을 다루는 책. 이 책은 동기·맥락으로만 참고하고, 실제 구현 로드맵은 표준 딥러닝/LLM 이론을 바탕으로 별도로 설계함
- **환경**: Python + numpy(3~7단계 손구현), PyTorch 자동미분(8단계+). 가상환경은 `.venv`.
- **실행/확인 방법**: 2026-09-14부터 VS Code(+Python 확장)를 설치해서 사용자가 직접 ▶ 버튼으로 코드를 실행하고 결과를 봄. 터미널(Terminal.app, MCP)은 한글 폰트가 필요한 matplotlib 시각화, git push, gh 명령 등 특수한 경우에만 사용.

## 관련 문서

- [PROGRESS.md](PROGRESS.md) — 지금 어디까지 왔나, 다음은 무엇인가
- [STUDY.md](STUDY.md) — 용어집 + 질문/답변 누적
- [PROBLEMS.md](PROBLEMS.md) — 아직 못 푼 문제
